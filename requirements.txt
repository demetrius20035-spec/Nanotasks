# Ядро оркестратора
duckdb>=1.0.0        # встроенная СУБД (не SQLite): SQL, ACID, один файл
PyYAML>=6.0          # конфиги и формат импорта TODO
httpx>=0.27          # HTTP-клиент к локальным моделям (Ollama/LM Studio) и API

# Десктоп-GUI (можно не ставить, если работаешь только через CLI)
PySide6>=6.6
