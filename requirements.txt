# Ядро оркестратора
duckdb>=1.0.0        # встроенная СУБД (не SQLite): SQL, ACID, один файл
PyYAML>=6.0          # конфиги и формат импорта TODO
httpx>=0.27          # HTTP-клиент к локальным моделям (Ollama/LM Studio) и ГигаЧату

# Нужен только для Яндекс-моделей (Алиса / YandexGPT) — Responses API.
openai>=1.50

# Десктоп-GUI (можно не ставить, если работаешь только через CLI)
PySide6>=6.6
